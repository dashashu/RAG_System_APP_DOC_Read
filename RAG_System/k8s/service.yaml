apiVersion: v1
kind: Service
metadata:
  name: rag-pdf
  labels:
    app: rag-pdf
spec:
  type: NodePort
  selector:
    app: rag-pdf
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
